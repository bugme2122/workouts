.superpowers/
node_modules/

# Secrets — never commit. server/.gitignore covers server/.env; this covers the root one.
.env
.env.local
.env.*.local

# Scratch / reference assets that don't belong in the repo (GAPS #4).
image.png
/ScottPlan-*.md

# Playwright MCP scratch output
.playwright-mcp/
