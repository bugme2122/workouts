{
  "name": "ladder-circuit-timer",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "npx --yes http-server -p 8000 -c-1",
    "dev": "npx --yes live-server --port=8000",
    "test": "node --test tests/*.mjs",
    "test:watch": "node --test --watch tests/*.mjs"
  }
}
